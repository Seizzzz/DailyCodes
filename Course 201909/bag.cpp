#include <bits/stdc++.h>
using namespace std;

int T;
int w[105];
void sol(int now)
{
	if(now == T) cout <<
	else if(now > T) return;
	
	//else now < T
	for(int i=0)
}

int main()
{
	std::ios::sync_with_stdio(false);
	
	cin >> T;
	int n; cin >> n;
	for(int i=0;i<n;++i) cin >> w[i];
	
	sol(now);
	

	return 0;
}

